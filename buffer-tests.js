const testBufferOutputMatches = (plugin, input, output) => {
  pending('todo');
}

const testBufferModeThrowsError = (plugin, input, error) => {
  pending('todo');
}

module.exports = {
  testBufferModeThrowsError,
  testBufferOutputMatches
}
